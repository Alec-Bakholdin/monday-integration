namespace monday_integration.src.api
{
    public enum LookupType
    {
        FABRIC_CONTENT,
        BRAND_NAME,
        BODY,
        STYLE_PRICE,
        STYLE_ID,
        ORIGIN_COUNTRY,
        SIZE_SCALE,
        WAREHOUSE_STATE,
        ORDER_ENTERED_DATE
    }
}